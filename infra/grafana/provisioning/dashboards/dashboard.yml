# Load JSON dashboards from the folder below when the container starts.
apiVersion: 1
providers:
  - name: ARCS
    type: file
    options:
      path: /etc/grafana/dashboards
      foldersFromFilesStructure: false
