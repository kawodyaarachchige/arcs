# Tells Grafana where Prometheus lives on the Docker network (service name: prometheus).
apiVersion: 1
datasources:
  - name: Prometheus
    uid: arcs-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
